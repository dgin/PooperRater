findRestroom = function(userCoords) {
    // pseudocode:
    var latitude = userCoords[0];
    var longitude = userCoords[1];
    var placeList = document.getElementsByClassName("")
    for (places in range(user.geocoordinates)
        if (place_rating === 5)
            return pick_one();
        else if (place_rating >= 4)
            return pick_one();
//        So on
};